# index operator[] = gives access to a sequence's element(str, list, tuples)

name = "Michal Bialas"

#if(name[0].islower()):
#    name = name.capitalize()
first_name = name[:6].upper()
last_name = name[7:].lower()
last_character = name[-1]
reverse=name[::-1].upper()
print(first_name)
print(last_name)
print(last_character)
print(reverse)
